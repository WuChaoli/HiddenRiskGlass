{
  /* demoOnlineonly flavor 覆盖项。
   * 用于保留原有 buildConfigField 的差异化行为。
   */
  "aiInspection": {
    // demo 版在线检测间隔更长，减少连续请求频率。单位 ms。
    "onlineDetectIntervalMs": 2000,
    // demo 版对本地隐患强制走在线详情，以便演示完整链路。
    "forceOnlineDetailForLocalHazard": true
  }
}
