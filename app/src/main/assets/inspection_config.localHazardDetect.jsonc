{
  /* localHazardDetect flavor 覆盖项。
   * 隐患识别页的自动 ctype=1 检测改为本地 NCNN，命中后等待远程 ctype=0 深度分析。
   */
  "aiInspection": {
    // 自动隐患检测只走本地 NCNN，不启动在线 ctype=1 轮询。
    "autoHazardRoutingMode": "LOCAL_ONLY",
    // 本地命中后强制请求远程深度分析，成功后再展示描述页。
    "forceOnlineDetailForLocalHazard": true
  }
}
