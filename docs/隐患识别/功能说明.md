# 隐患识别功能说明

## 功能目标

完成从加载初始化、检测态识别、结果态呈现到保存/返回的完整隐患识别能力。

## 功能边界

- `InspectionLoadingActivity` 负责初始化与分流
- `AiInspectionActivity` 负责检测态与结果态的识别交互
- 自动链路策略、会话与输入规则依赖公共能力文档

## 关键约束

- `InspectionSession.isInitialized` 是页面可直进门槛
- 远端优先，本地模型作为备用链路
- 结果态应隐藏巡检功能菜单
