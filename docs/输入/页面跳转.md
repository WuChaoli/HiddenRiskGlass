# 输入页面跳转

输入本身不负责页面跳转，但它决定页面在什么状态下能触发哪些跳转动作。

当前关键例子：

- `AiInspectionActivity` 检测态可语音跳到 `DeviceGuideActivity`、`HazardRecordActivity`
- `DeviceGuideActivity` 检测态可语音跳到 `AiInspectionActivity`、`HazardRecordActivity`
- `HazardRecordActivity` 空闲态可语音跳到 `DeviceGuideActivity`
