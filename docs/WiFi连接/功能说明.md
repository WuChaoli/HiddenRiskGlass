# WiFi连接功能说明

## 功能目标

在企业巡检主链中完成 Wi-Fi 扫码配网，并在连接验证成功后进入后续企业扫码链路。

## 功能边界

- 负责读取 Wi-Fi 二维码
- 负责发起系统配网或 `WifiNetworkSpecifier` 回退
- 负责校验当前 SSID 是否已切换
- 不负责企业对象信息获取和菜单分发

## 输出结果

- 连接成功后调用 `InspectionWorkflowSession.updateMode(connected = true)`
- 根据 `EXTRA_NEXT_AFTER_SUCCESS` 跳转到下一页
