# WiFi连接页面跳转

- `InspectionLoadingActivity` 在企业巡检开启且未连接 Wi-Fi 时，进入 `WifiQrScanActivity`
- `WifiQrScanActivity` 成功连接后，按 `EXTRA_NEXT_AFTER_SUCCESS` 进入后续页面
- 当前正式主链中，下一页通常是 `EnterpriseQrScanActivity`
- 用户主动取消时，当前实现直接退出应用任务
