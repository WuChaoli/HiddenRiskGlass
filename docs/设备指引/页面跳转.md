# 设备指引页面跳转

- 主菜单和加载页可进入 `DeviceGuideActivity`
- `AiInspectionActivity` 与 `HazardRecordActivity` 可通过语音跳到 `DeviceGuideActivity`
- `DeviceGuideActivity` 可通过语音跳到：
  - `AiInspectionActivity`
  - `HazardRecordActivity`
- 结束任务时进入 `InspectionEndReportActivity`
