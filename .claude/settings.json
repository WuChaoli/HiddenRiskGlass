{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "sh",
            "args": [
              "-c",
              "printf '%s\\n' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"additionalContext\":\"【文档同步提醒】检测到 git commit 操作。提交前请先执行 /codexmap-sync 同步三层文档体系（CLAUDE.md → CODEMAPS.md → 模块 README），确认文档与代码变更一致后再继续提交。\"}}'"
            ]
          }
        ]
      }
    ]
  }
}
